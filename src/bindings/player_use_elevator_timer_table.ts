// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit 080c501f238afc5fc0ccaedf8742ff4fe306727a).

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  Identity,
  ProductType,
  ProductTypeElement,
  SubscriptionBuilderImpl,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
  type CallReducerFlags,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  type ReducerEventContextInterface,
  type SubscriptionEventContextInterface,
} from "@clockworklabs/spacetimedb-sdk";
import { PlayerUseElevatorTimer } from "./player_use_elevator_timer_type";
import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `player_use_elevator_timer`.
 *
 * Obtain a handle from the [`playerUseElevatorTimer`] property on [`RemoteTables`],
 * like `ctx.db.playerUseElevatorTimer`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.playerUseElevatorTimer.on_insert(...)`.
 */
export class PlayerUseElevatorTimerTableHandle {
  tableCache: TableCache<PlayerUseElevatorTimer>;

  constructor(tableCache: TableCache<PlayerUseElevatorTimer>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<PlayerUseElevatorTimer> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `scheduledId` unique index on the table `player_use_elevator_timer`,
   * which allows point queries on the field of the same name
   * via the [`PlayerUseElevatorTimerScheduledIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.playerUseElevatorTimer.scheduledId().find(...)`.
   *
   * Get a handle on the `scheduledId` unique index on the table `player_use_elevator_timer`.
   */
  scheduledId = {
    // Find the subscribed row whose `scheduledId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: bigint): PlayerUseElevatorTimer | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.scheduledId, col_val)) {
          return row;
        }
      }
    },
  };
  /**
   * Access to the `playerEntityId` unique index on the table `player_use_elevator_timer`,
   * which allows point queries on the field of the same name
   * via the [`PlayerUseElevatorTimerPlayerEntityIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.playerUseElevatorTimer.playerEntityId().find(...)`.
   *
   * Get a handle on the `playerEntityId` unique index on the table `player_use_elevator_timer`.
   */
  playerEntityId = {
    // Find the subscribed row whose `playerEntityId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: bigint): PlayerUseElevatorTimer | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.playerEntityId, col_val)) {
          return row;
        }
      }
    },
  };
  /**
   * Access to the `originPlatformEntityId` unique index on the table `player_use_elevator_timer`,
   * which allows point queries on the field of the same name
   * via the [`PlayerUseElevatorTimerOriginPlatformEntityIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.playerUseElevatorTimer.originPlatformEntityId().find(...)`.
   *
   * Get a handle on the `originPlatformEntityId` unique index on the table `player_use_elevator_timer`.
   */
  originPlatformEntityId = {
    // Find the subscribed row whose `originPlatformEntityId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: bigint): PlayerUseElevatorTimer | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.originPlatformEntityId, col_val)) {
          return row;
        }
      }
    },
  };
  /**
   * Access to the `destinationPlatformEntityId` unique index on the table `player_use_elevator_timer`,
   * which allows point queries on the field of the same name
   * via the [`PlayerUseElevatorTimerDestinationPlatformEntityIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.playerUseElevatorTimer.destinationPlatformEntityId().find(...)`.
   *
   * Get a handle on the `destinationPlatformEntityId` unique index on the table `player_use_elevator_timer`.
   */
  destinationPlatformEntityId = {
    // Find the subscribed row whose `destinationPlatformEntityId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: bigint): PlayerUseElevatorTimer | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.destinationPlatformEntityId, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: PlayerUseElevatorTimer) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: PlayerUseElevatorTimer) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: PlayerUseElevatorTimer) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: PlayerUseElevatorTimer) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: PlayerUseElevatorTimer, newRow: PlayerUseElevatorTimer) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: PlayerUseElevatorTimer, newRow: PlayerUseElevatorTimer) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
