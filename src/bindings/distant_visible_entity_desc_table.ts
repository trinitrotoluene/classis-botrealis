// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit 080c501f238afc5fc0ccaedf8742ff4fe306727a).

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  Identity,
  ProductType,
  ProductTypeElement,
  SubscriptionBuilderImpl,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
  type CallReducerFlags,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  type ReducerEventContextInterface,
  type SubscriptionEventContextInterface,
} from "@clockworklabs/spacetimedb-sdk";
import { DistantVisibleEntityDesc } from "./distant_visible_entity_desc_type";
import { EntityType as __EntityType } from "./entity_type_type";

import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `distant_visible_entity_desc`.
 *
 * Obtain a handle from the [`distantVisibleEntityDesc`] property on [`RemoteTables`],
 * like `ctx.db.distantVisibleEntityDesc`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.distantVisibleEntityDesc.on_insert(...)`.
 */
export class DistantVisibleEntityDescTableHandle {
  tableCache: TableCache<DistantVisibleEntityDesc>;

  constructor(tableCache: TableCache<DistantVisibleEntityDesc>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<DistantVisibleEntityDesc> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `id` unique index on the table `distant_visible_entity_desc`,
   * which allows point queries on the field of the same name
   * via the [`DistantVisibleEntityDescIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.distantVisibleEntityDesc.id().find(...)`.
   *
   * Get a handle on the `id` unique index on the table `distant_visible_entity_desc`.
   */
  id = {
    // Find the subscribed row whose `id` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): DistantVisibleEntityDesc | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.id, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: DistantVisibleEntityDesc) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: DistantVisibleEntityDesc) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: DistantVisibleEntityDesc) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: DistantVisibleEntityDesc) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: DistantVisibleEntityDesc, newRow: DistantVisibleEntityDesc) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: DistantVisibleEntityDesc, newRow: DistantVisibleEntityDesc) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
