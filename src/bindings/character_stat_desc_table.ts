// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

// This was generated using spacetimedb cli version 1.3.0 (commit 080c501f238afc5fc0ccaedf8742ff4fe306727a).

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  Identity,
  ProductType,
  ProductTypeElement,
  SubscriptionBuilderImpl,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
  type CallReducerFlags,
  type DbContext,
  type ErrorContextInterface,
  type Event,
  type EventContextInterface,
  type ReducerEventContextInterface,
  type SubscriptionEventContextInterface,
} from "@clockworklabs/spacetimedb-sdk";
import { CharacterStatDesc } from "./character_stat_desc_type";
import { type EventContext, type Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `character_stat_desc`.
 *
 * Obtain a handle from the [`characterStatDesc`] property on [`RemoteTables`],
 * like `ctx.db.characterStatDesc`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.characterStatDesc.on_insert(...)`.
 */
export class CharacterStatDescTableHandle {
  tableCache: TableCache<CharacterStatDesc>;

  constructor(tableCache: TableCache<CharacterStatDesc>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<CharacterStatDesc> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `statType` unique index on the table `character_stat_desc`,
   * which allows point queries on the field of the same name
   * via the [`CharacterStatDescStatTypeUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.characterStatDesc.statType().find(...)`.
   *
   * Get a handle on the `statType` unique index on the table `character_stat_desc`.
   */
  statType = {
    // Find the subscribed row whose `statType` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: number): CharacterStatDesc | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.statType, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: CharacterStatDesc) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: CharacterStatDesc) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: CharacterStatDesc) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: CharacterStatDesc) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: CharacterStatDesc, newRow: CharacterStatDesc) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: CharacterStatDesc, newRow: CharacterStatDesc) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
